global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "mcp-prism"
    static_configs:
      - targets: ["mcp-prism:8787"]
    metrics_path: /metrics

